describe("ClientViewErrorMessageService", () => {
    test("github action test counter measure", () => {
        expect(1).toBe(1)
    })
    // process.env.CHOBIIT_LANG = lang;
    // const { ClientViewErrorMessageService } = require("./client-view-error-message-service")

    // const translateKey = 'target-list-view-not-found'
    // const translatedErrorMessage = ClientViewErrorMessageService.getErrorMessage(translateKey)
    // console.log("translatedErrorMessage", translatedErrorMessage)
    // expect(translatedErrorMessage).toBe(targetListViewNotFound)

    // it('should return translated error message when translateKey is target-list-view-not-found.', () => {
    //     const translateKey = 'target-list-view-not-found'
    //     const translatedErrorMessage = ClientViewErrorMessageService.getErrorMessage(translateKey)
    //     expect(translatedErrorMessage).toBe(targetListViewNotFound)
    // })
    // it('should return translated error message when translateKey is all-list-view-is-not-setting', () => {
    //     const translateKey = 'all-list-view-is-not-setting'
    //     const translatedErrorMessage = ClientViewErrorMessageService.getErrorMessage(translateKey)
    //     expect(translatedErrorMessage).toBe(allListViewIsNotSetting)
    // })
    // it('should return translated error message when translateKey is not defined', () => {
    //     const translateKey = 'error'
    //     const translatedErrorMessage = ClientViewErrorMessageService.getErrorMessage(translateKey)
    //     expect(translatedErrorMessage).toBe(cannotGetAppConfig)
    // })
    // it('should return translated error message when translateKey is null', () => {
    //     const translateKey = null
    //     const translatedErrorMessage = ClientViewErrorMessageService.getErrorMessage(translateKey)
    //     expect(translatedErrorMessage).toBe(cannotGetAppConfig)
    // })
    
})